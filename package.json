{
  "name": "@chloerei/material-ui",
  "version": "0.0.10",
  "description": "A Material Design front-end component library.",
  "main": "dist/js/material-ui.js",
  "style": "dist/css/material-ui.css",
  "scripts": {
    "build": "npm run build:js & npm run build:js-bundle & npm run build:css",
    "build:js": "npx esbuild src/js/material-ui.js --outfile=dist/material-ui.js --format=esm --bundle --external:stimulus",
    "build:js-bundle": "npx esbuild src/js/material-ui.js --outfile=dist/material-ui.bundle.js --format=esm --bundle",
    "build:css": "npx sass src/css/material-ui.scss dist/material-ui.css --no-source-map"
  },
  "files": [
    "dist/*.{js,css,map}",
    "src/**/*.{js,scss}"
  ],
  "repository": "github:chloerei/material-ui",
  "author": "Rei",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/chloerei/material-ui/issues"
  },
  "homepage": "https://github.com/chloerei/material-ui",
  "hexo": {
    "version": "5.4.0"
  },
  "peerDependencies": {
    "@hotwired/stimulus": "3.x"
  },
  "devDependencies": {
    "@hotwired/stimulus": "^3.0.1",
    "esbuild": "^0.13.8",
    "hexo": "^5.0.0",
    "hexo-renderer-ejs": "^1.0.0",
    "hexo-renderer-marked": "^2.0.0",
    "hexo-server": "^1.0.0",
    "sass": "^1.43.3"
  }
}
